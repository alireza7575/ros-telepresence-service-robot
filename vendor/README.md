# Vendor

Copied third-party ROS packages kept for historical reproducibility.

- `async_web_server_cpp/`
- `web_video_server/`
- `usb_cam/`

For a production ROS project, these should be installed as dependencies or managed as submodules instead of copied into the repository.
