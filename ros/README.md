# ROS

ROS files owned by this project.

- `packages/ros_bridge/` contains the custom ROS bridge package and scripts.
- `launch/thermal_cam.launch` contains the project-owned thermal camera launch file.

Vendored third-party ROS packages are kept separately under `vendor/`.
