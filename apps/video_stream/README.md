# Video Stream Apps

Flask/OpenCV camera-streaming experiments.

- `ubuntu/` contains the Ubuntu-oriented variant.
- `windows/` contains the Windows-oriented variant.

These folders were kept separate because the original prototype had platform-specific camera and runtime behavior.
