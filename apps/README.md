# Apps

Project-owned runtime applications.

- `launcher/` starts and stops ROS, camera, audio, and serial services from a Tkinter UI.
- `audio/` contains the socket audio client/server prototype.
- `video_stream/` contains Flask/OpenCV video-streaming experiments for Ubuntu and Windows.
